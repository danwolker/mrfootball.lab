import React from "react";
import "../styles/Header.css";

const Header: React.FC = () => {
    return (
        <header>
        <h1>Header</h1>
        </header>
    );
    };  

export default Header;